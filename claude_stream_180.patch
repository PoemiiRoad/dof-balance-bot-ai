--- main.py (current)
+++ main.py (streaming + 180s)
@@ -37,6 +37,7 @@
     TextBlock,
     query,
 )
+from claude_agent_sdk.types import StreamEvent
 from dotenv import load_dotenv
 
 from balance_logic import (
@@ -58,7 +59,7 @@
 logging.basicConfig(level=logging.INFO, format="%(asctime)s %(levelname)s %(message)s")
 logger = logging.getLogger(__name__)
 
-BUILD_VERSION = "2026.09.15-railway-formulas-windows-v2"
+BUILD_VERSION = "2026.09.15-claude-stream-180-v1"
 
 # ════════════════════════════════════════════════════════
 #  НАСТРОЙКИ
@@ -83,7 +84,7 @@
 )
 
 try:
-    AI_TIMEOUT_SECONDS = int(os.getenv("AI_TIMEOUT_SECONDS", "90"))
+    AI_TIMEOUT_SECONDS = int(os.getenv("AI_TIMEOUT_SECONDS", "180"))
 except ValueError as exc:
     raise RuntimeError("AI_TIMEOUT_SECONDS должен быть целым числом") from exc
 if not 30 <= AI_TIMEOUT_SECONDS <= 300:
@@ -1559,8 +1560,9 @@
 
 
 async def _run_claude_agent(prompt: str) -> tuple[str, str | None, int | None]:
-    """Один изолированный запрос к Claude без инструментов и памяти сессии."""
+    """Один изолированный streaming-запрос к Claude без инструментов и памяти."""
     text_parts: list[str] = []
+    stream_parts: list[str] = []
     final_result: ResultMessage | None = None
     assistant_error: str | None = None
 
@@ -1573,6 +1575,7 @@
         max_turns=1,
         setting_sources=[],
         skills=[],
+        include_partial_messages=True,
         env={
             "CLAUDE_CODE_OAUTH_TOKEN": CLAUDE_CODE_OAUTH_TOKEN,
             "CLAUDE_CODE_DISABLE_AUTO_MEMORY": "1",
@@ -1583,7 +1586,15 @@
 
     try:
         async for message in query(prompt=prompt, options=options):
-            if isinstance(message, AssistantMessage):
+            if isinstance(message, StreamEvent):
+                event = message.event
+                if event.get("type") == "content_block_delta":
+                    delta = event.get("delta", {})
+                    if delta.get("type") == "text_delta":
+                        chunk = delta.get("text", "")
+                        if chunk:
+                            stream_parts.append(chunk)
+            elif isinstance(message, AssistantMessage):
                 if message.error:
                     assistant_error = str(message.error)
                 text_parts.extend(
@@ -1622,8 +1633,11 @@
     answer = ""
     if final_result and final_result.result:
         answer = final_result.result.strip()
+    if not answer and stream_parts:
+        answer = "".join(stream_parts).strip()
     if not answer:
         answer = "\n".join(text_parts).strip()
+    logger.info("Claude Agent stream completed: %s text chunks", len(stream_parts))
     return answer, assistant_error, None
 
 
